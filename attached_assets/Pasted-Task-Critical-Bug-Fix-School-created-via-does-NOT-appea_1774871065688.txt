Task: Critical Bug Fix — School created via "تسجيل جديد" does NOT appear in Platform Admin "إدارة المدارس"

Severity: HIGH (Core system inconsistency)

Problem Description:
When a School Principal registers through the "تسجيل جديد" flow, the school is NOT visible in the Platform Admin dashboard under "إدارة المدارس".

This creates a critical inconsistency between user registration and tenant/school management.

Expected Behavior:
- Any school created via "تسجيل جديد" must:
  1) Be fully created in the database
  2) Be linked correctly to a valid SchoolId (tenant)
  3) Be linked to the Principal account
  4) Appear immediately in Platform Admin → "إدارة المدارس"
- If there is a status system (Pending / Draft / Active / Under Review), the school MUST still appear in the appropriate list.

Your Mission:
Fix this issue ROOT-CAUSE level. No temporary fixes or UI-only patches are acceptable.

Investigation Scope:
You MUST trace the full flow end-to-end:
1) Frontend:
   - Registration form ("تسجيل جديد")
   - Payload structure
   - API call

2) Backend:
   - Endpoint responsible for registration
   - Logic for creating:
     - User (Principal)
     - School entity
     - Tenant / SchoolId
   - Validation rules
   - Status assignment

3) Database:
   - Verify if school is:
     - Not created at all
     - Created but missing fields
     - Created but not linked to principal
     - Created but missing tenant context
     - Created but hidden بسبب status/filter

4) Admin Dashboard:
   - Endpoint used by "إدارة المدارس"
   - Filters applied (status, tenant, role, etc.)
   - Ensure no incorrect filtering hides the school

Possible Root Causes to Check:
- School entity not created during registration
- Principal created without school linkage
- Missing or null SchoolId / tenant reference
- Incorrect status value causing filtering
- API returning incomplete data
- Admin list endpoint filtering out records incorrectly
- Field name mismatch between frontend and backend

Required Fix:
- Ensure registration flow creates:
  - Principal user
  - School entity
  - Tenant (SchoolId)
  in a SINGLE consistent flow
- Ensure proper linking between all entities
- Ensure school appears in admin list regardless of status (within correct category)
- Normalize field names across frontend/backend
- Add defensive validation (fail clearly if school creation fails)
- Add logging for debugging failures

Testing (MANDATORY):
1) Register a new principal via "تسجيل جديد"
   → Confirm:
     - user created
     - school created
     - tenant created
     - school visible in "إدارة المدارس"

2) Refresh page
   → School must still appear

3) Check DB:
   → No orphan users or orphan schools

4) Test edge cases:
   → Missing fields
   → duplicate email/school

Output Required:
1) Root cause identified
2) Exact fix implemented
3) Files/endpoints modified
4) Confirmation of successful tests

CRITICAL REQUIREMENT:
Do NOT patch symptoms.
Do NOT rely on frontend-only fixes.
Fix must be systemic, consistent, and production-safe.