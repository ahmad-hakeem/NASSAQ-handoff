Implement a Bulk Actions system inside the "Product Intelligence Hub" → "Challenges" tab to allow efficient multi-selection and batch operations on challenge cards.

==================================================
1) CORE OBJECTIVE
==================================================

Enable users (specifically authorized admins) to:

→ select multiple challenge cards  
→ perform batch actions efficiently  
→ reduce repetitive manual operations  

This should transform the page into:
→ operational control panel

==================================================
2) TARGET USERS (STRICT PERMISSION)
==================================================

Bulk actions must be available ONLY for:

- zalat@nassaqapp.com
- hakim@nassaqapp.com

All other users:
→ must NOT see or access bulk actions

==================================================
3) SELECTION SYSTEM (CRITICAL)
==================================================

--------------------------------------------------
A) CHECKBOX UI
--------------------------------------------------

- Add a checkbox to each challenge card
- Position:
  → top corner of the card (consistent location)

- Add:
  → "Select All" checkbox at the top of the list

--------------------------------------------------
B) SELECTION BEHAVIOR
--------------------------------------------------

- User can:
  → select one or multiple cards
  → toggle selection individually
  → select all cards

- Selected cards must:
  → show clear visual state (highlight / border)

==================================================
4) BULK ACTION BAR (APPEARS ON SELECTION)
==================================================

- When at least one card is selected:
  → show a sticky/top action bar

The bar must display:
- number of selected items
- available actions

Example:
"3 challenges selected"

==================================================
5) BULK ACTIONS (MANDATORY)
==================================================

Provide the following actions:

--------------------------------------------------
A) BULK EDIT
--------------------------------------------------

Allow updating for selected cards:

- Status
- Priority
- Assignment (user/team)

Behavior:
- open modal or side panel
- apply changes to all selected cards

--------------------------------------------------
B) BULK DELETE (CRITICAL)
--------------------------------------------------

- Allow deleting multiple challenges

Requirements:
- must show confirmation modal:
  → "Are you sure you want to delete X challenges?"

- require:
  → explicit confirmation (button click)

Optional:
- second confirmation for safety

--------------------------------------------------
C) OPTIONAL ACTIONS (IF NEEDED)
--------------------------------------------------

- Mark as Duplicate
- Change category/type
- Trigger Hakim analysis (bulk)

==================================================
6) PERMISSION ENFORCEMENT (CRITICAL)
==================================================

Frontend:
- Bulk actions UI visible ONLY for:
  - zalat@nassaqapp.com
  - hakim@nassaqapp.com

Backend:
- Validate all bulk requests
- Reject any unauthorized action

==================================================
7) VISUAL FEEDBACK
==================================================

- Selected cards:
  → highlighted background or border

- After action:
  → show success toast:
     - "تم تحديث 3 تحديات"
     - "تم حذف 2 تحدي"

- Update UI instantly (optimistic UI if possible)

==================================================
8) ERROR HANDLING
==================================================

If action fails:

- show clear error message
- indicate which items failed (if partial success)

==================================================
9) PERFORMANCE
==================================================

- Bulk operations must:
  → be efficient
  → handle large selections

- Avoid:
  → multiple sequential API calls

Use:
→ batch API endpoint

==================================================
10) RESPONSIVENESS
==================================================

Desktop:
→ full action bar

Mobile:
→ compact action bar (bottom or top)

==================================================
11) UX RULES
==================================================

- Selection must feel:
  → fast
  → intuitive
  → predictable

- Avoid:
  → accidental selection
  → hidden actions

==================================================
12) FINAL GOAL
==================================================

Transform the page from:

→ manual single-item management

To:

→ powerful bulk operation system

Where admins can:
- act faster
- reduce effort
- manage multiple challenges efficiently