Customize the external (preview) view of Challenge Cards inside the "Product Intelligence Hub" page specifically for super-admin accounts, enforcing a minimal and focused display.

==================================================
1) TARGET USERS (STRICT SCOPE)
==================================================

This behavior must apply ONLY to the following accounts:

- zalat@nassaqapp.com
- hakim@nassaqapp.com

All other users:
→ must continue using the current/default card design without any changes

==================================================
2) CORE OBJECTIVE
==================================================

For the specified admin accounts, simplify the card preview to show only essential, decision-critical information.

The card should act as:
→ minimal control panel
→ quick scanning + quick navigation

==================================================
3) DISPLAYED DATA (ONLY THESE FIELDS)
==================================================

Each card must display ONLY the following:

- Challenge Title (العنوان)
- Reporter (المُبلّغ / الحساب المبلغ)
- Status (الحالة)
- Duplicate Indicator:
  → clearly show if:
     - "مكرر"
     - "غير مكرر"
- Challenge ID / Serial Number (رقم التحدي)

DO NOT display:
- progress bar
- comments preview
- Hakim analysis
- extra metadata
- descriptions
- any secondary information

==================================================
4) REMOVE EXPAND / COLLAPSE BEHAVIOR
==================================================

- Disable completely:
  → expand
  → collapse
  → inline detailed view

- Card must remain:
  → static preview only

==================================================
5) PRIMARY ACTION (MANDATORY)
==================================================

Each card must include a clear and visible button:

👉 "فتح الصفحة الكاملة" / "Open Full Page"

Behavior:
- Clicking this button navigates to the full challenge details page
- This becomes the ONLY interaction method for deep access

==================================================
6) VISUAL DESIGN
==================================================

- Keep layout:
  → clean
  → structured
  → minimal

- Ensure:
  → strong readability
  → proper spacing between elements
  → clear hierarchy (Title → Status → Reporter → ID)

- Duplicate indicator must be:
  → visually noticeable
  → easy to scan

==================================================
7) ROLE-BASED UI ENFORCEMENT
==================================================

Frontend:
- Render minimal card view ONLY for the two specified accounts
- All other users must not see this layout

Backend (if applicable):
- Ensure role/identity-based rendering logic is enforced correctly

==================================================
8) CONSISTENCY
==================================================

- All cards for admin users must follow identical structure
- No variation between cards

==================================================
9) FINAL GOAL
==================================================

For admin accounts:

Transform the card from:

→ detailed preview card

To:

→ minimal decision-focused control card

Where the user can:
- quickly scan key data
- identify duplication and status
- directly navigate to full details

Without distraction or unnecessary information