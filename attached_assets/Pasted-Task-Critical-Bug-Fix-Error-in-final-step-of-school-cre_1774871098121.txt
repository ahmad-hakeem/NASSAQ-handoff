Task: Critical Bug Fix — Error in final step of school creation (Platform Admin)

Severity: HIGH (Breaks core admin workflow)

Problem Description:
When creating a new school from the Platform Admin account, an error occurs at the FINAL step, preventing successful completion.

Expected Behavior:
- Multi-step school creation must:
  1) Complete successfully at final step
  2) Persist all data correctly in database
  3) Show clear success confirmation
  4) Immediately display the new school in "إدارة المدارس"
- No silent failures or partial/inconsistent saves

Your Mission:
Fix this issue COMPLETELY from root cause.
No temporary fixes, no bypasses.

Investigation Scope:
You MUST deeply analyze:

1) Frontend (Multi-step form):
   - Step state management
   - Data accumulation across steps
   - Final payload construction
   - API call at final step
   - Any lost state or missing fields

2) Backend:
   - Endpoint handling school creation
   - Required fields validation
   - Schema enforcement
   - Transaction handling (is it partial save?)

3) API:
   - Check request payload vs expected schema
   - Identify missing / malformed fields
   - Response errors

4) Database:
   - Is the school partially created?
   - Is there rollback failure?
   - Are required fields missing?

5) Error Handling:
   - Is error swallowed (silent)?
   - Is UI showing incorrect message?
   - Is API returning proper error?

Possible Root Causes to Check:
- Missing required field in final step
- Multi-step form losing data before submission
- Payload mismatch with backend schema
- Validation failure at backend
- Improper async handling
- Transaction not atomic (partial save)
- API error not handled properly
- State reset before submission

Required Fix:
- Ensure final step sends COMPLETE and VALID payload
- Ensure backend validates and returns clear errors
- Ensure atomic operation (no partial saves)
- Fix frontend state persistence across steps
- Normalize schema between frontend and backend
- Add clear success + error UX messages (Arabic)
- Prevent silent failure completely

Enhancements (Important):
- Add logging for failed submissions
- Add validation BEFORE submission (frontend)
- Add defensive validation (backend)
- Ensure consistent response format

Testing (MANDATORY):
1) Create new school from admin
   → Complete all steps
   → Final step must succeed

2) Verify:
   - School saved in DB
   - School appears in "إدارة المدارس"

3) Refresh page
   → Data must persist

4) Negative testing:
   - Missing required fields
   - Invalid data
   - Network failure

5) No partial records in DB

Output Required:
1) Root cause identified
2) Exact fix implemented
3) Files/components/endpoints modified
4) Confirmation of successful tests

CRITICAL REQUIREMENT:
- Fix must be FULLY reliable
- No workaround or retry-based solution
- No UI-only patch
- Must be stable for production