# نَسَّق | NASSAQ - Product Requirements Document

## نظرة عامة
نظام إدارة مدارس متكامل يعمل بالذكاء الاصطناعي، متعدد المستأجرين (Multi-tenant)

## المستخدمون
1. **مدير المنصة (Platform Admin)** - إدارة جميع المدارس والمستخدمين
2. **مدير المدرسة (School Principal)** - إدارة مدرسة واحدة
3. **المعلم (Teacher)** - إدارة الفصول والحصص
4. **الطالب (Student)** - عرض الجدول والدرجات
5. **ولي الأمر (Parent)** - متابعة أداء الأبناء

---

## الميزات المُنجزة

### إصلاحات حرجة - شبكة الجدول وإسناد الفصول ✅ (مارس 2026)

**المشاكل المُصلحة:**

1. ✅ **شبكة الجدول الدراسي لا تعرض البيانات بعد التوليد:**
   - **السبب:** استخدام `visibleSlots` و `Calendar` قبل تعريفهما في `TimetableGridSection.jsx`
   - **الإصلاح:** نقل تعريف `visibleSlots` قبل استخدامه، إضافة استيراد `Calendar` من lucide-react
   - **النتيجة:** الجدول يعرض 60 حصة مجدولة بشكل صحيح مع 5 أيام عمل و7 حصص

2. ✅ **نافذة التأكيد لا تعرض البيانات الفعلية:**
   - **الموقع:** `TimetableModals.jsx` - `AITimetableGenerationModal`
   - **الإصلاح:** التأكد من عرض `generationInputSummary` بشكل صحيح
   - **النتيجة:** النافذة تعرض: 8 معلم، 8 فصل، 11 مادة، 40 حصة

3. ✅ **إسناد الفصول للمعلمين يجب أن يستخدم السحب والإفلات:**
   - **الموقع:** `SchoolSettingsPagePro.jsx`
   - **الإصلاح:** إضافة استيرادات `@dnd-kit/core` (DndContext, DragOverlay, closestCenter, useSensor, useSensors, PointerSensor, useDraggable, useDroppable)
   - **المكونات الجديدة:** `DraggableClassItem`, `DroppableTeacherBox`
   - **النتيجة:** واجهة سحب وإفلات كاملة لإسناد الفصول للمعلمين

**الملفات المُعدّلة:**
- `/app/frontend/src/components/timetable/TimetableGridSection.jsx`
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx`

**نتيجة الاختبار:** 17/17 اختبار ناجح (100%)

---

### مركز القيادة (Command Center) ✅ (محدّث مارس 2026)
- أزرار موحدة: "طالب / طلاب"، "معلم / معلمين"، "فصل / فصول"
- كروت مؤشرات الأداء (الطلاب، المعلمين، الفصول) قابلة للنقر
- قسم "يحتاج تدخل الآن" للمشكلات العاجلة
- نسبة الحضور اليوم (المعلمين والطلاب)
- **جميع الكروت تعرض بيانات ديناميكية من قاعدة البيانات فقط**
- **كارت المعلمين يعرض الرقم الصحيح (10) من قاعدة البيانات**
- **API جديدة: `/api/admin/command-center/stats`**

### تنسيق رأس الصفحات الموحد ✅ (مارس 2026)
- اسم المستخدم يظهر أولاً وبخط أكبر (text-2xl)
- عنوان الصفحة يظهر تحت الاسم (text-base)
- تم تطبيقه على: مركز القيادة، إعدادات المدرسة، إدارة المستخدمين، الجدول المدرسي

### الجدول المدرسي ✅
- زر "معالجة الجدول بالذكاء الاصطناعي" (بدلاً من "توليد")
- إزالة زر "+جديد"
- تكامل مع إعدادات المدرسة

### إدارة المستخدمين والفصول ✅ (محدّث مارس 2026)
- أزرار موحدة مع مركز القيادة
- فلتر تلقائي عند النقر من الكروت
- **عرض ديناميكي للبيانات من قاعدة البيانات فقط**
- **اسم المستخدم يظهر أولاً في رأس الصفحة**

### إدارة حضور المعلمين والاداريين ✅
- تغيير الاسم من "إدارة الحضور"
- تقارير نسب الحضور الفعلية

### مركز التواصل والاشعارات ✅
- تغيير "الرسائل" إلى "الرسائل المستلمة"
- كروت قابلة للنقر لعرض التفاصيل

### صفحة إعدادات المدرسة - مصدر الحقيقة الوحيد ✅ (محدّث مارس 2026)

**المبدأ الأساسي:** Single Source of Truth - صفحة إعدادات المدرسة هي المصدر الرسمي الوحيد لبيانات المدرسة

**إعادة الهيكلة الكاملة (مارس 2026):**
تم تقسيم الصفحة إلى قسمين رئيسيين:

#### القسم الأول: البيانات المتغيرة لبناء الجدول (Dynamic Timetable Inputs)
هذا القسم يحتوي على المدخلات التشغيلية لمحرك الجدول:
- **العام والفصل الدراسي** - اختيار العام والفصل الدراسي الحالي
- **أيام العمل** - تحديد أيام الدراسة والعطلة (7 أيام تفاعلية)
- **التوقيت والحصص** - وقت بداية اليوم، عدد الحصص، مدة الحصة
- **الاستراحات والصلاة** - فترات الاستراحة وصلاة الظهر
- **الفصول والشعب** - إدارة الفصول الدراسية
- **إسناد المعلمين** - ربط المعلمين بالمواد
- **عدم التوفر** - قيود توفر المعلمين والفصول
- **القيود والتفضيلات** - القيود الإلزامية (Hard) والتفضيلية (Soft)

#### القسم الثاني: البيانات الأساسية الثابتة العامة (Official Static Reference Data)
بيانات مرجعية رسمية للقراءة فقط:
- **المنهج الرسمي** - إحصائيات المنهج الدراسي من وزارة التعليم
- **المراحل والمسارات** - المراحل الدراسية (3) والمسارات التعليمية (8)
- **النصاب التعليمي** - نصاب كل رتبة معلم (حصص/أسبوع)
- **توزيع المواد** - توزيع المواد لكل صف

#### بطاقة جاهزية الجدول (Readiness Card) ✅
- **عنوان محدث:** "بيانات الجدول غير مكتملة" / "بيانات الجدول جاهزة جزئياً" / "جاهز لإنشاء الجدول!"
- **عرض النسبة المئوية** في دائرة تقدم
- **عرض البيانات المطلوبة** في كروت ملونة مع:
  - اسم الفئة (السياق الأكاديمي، أيام الدراسة، هيكل اليوم، إلخ)
  - النقاط المكتسبة/الإجمالية
  - قائمة بالمشاكل الحرجة
  - **زر "إصلاح"** للانتقال المباشر للتبويب المناسب
- **عرض البيانات المكتملة** بشارات خضراء
- **تحديث تلقائي للنسبة** بعد حفظ أي تعديلات

**الإصلاحات الحرجة المُنفذة:**
1. ✅ **CRUD المواد الدراسية:**
   - جلب المواد من `/school/subjects` بدلاً من `/reference/subjects`
   - تعديل وحذف المواد يعمل بشكل صحيح
   
2. ✅ **CRUD القيود الإدارية:**
   - إصلاح: POST كان يحفظ في `admin_constraints` بينما PUT/DELETE يبحثان في `school_constraints`
   - الآن جميع العمليات تستخدم `school_constraints` موحداً
   
3. ✅ **تعديل الأوقات المباشر (Inline Edit):**
   - مكون جديد `TimeSlotInlineEdit` للتعديل المباشر
   - إعادة حساب تلقائي متسلسل للفترات اللاحقة
   - حفظ فوري في قاعدة البيانات عند الـ blur أو Enter
   - زر "إعادة الضبط" للوضع الافتراضي
   
4. ✅ **تصميم بطاقة أيام العمل:**
   - كروت صغيرة لكل يوم من الأسبوع
   - ألوان مميزة (أخضر للدراسة، رمادي للعطلة)
   - دوائر إحصائية للملخص

**APIs المستخدمة:**
- `GET /school/settings` - جلب جميع الإعدادات
- `PUT /school/settings` - تحديث الإعدادات
- `GET/POST/PUT/DELETE /school/subjects` - إدارة المواد
- `GET/POST/PUT/DELETE /school/constraints` - إدارة القيود
- `GET /school/settings/stages` - المراحل التعليمية
- `GET /school/settings/audit-logs` - سجل التعديلات
- `GET /api/official-curriculum/stats` - إحصائيات المنهج الرسمي
- `GET /api/official-curriculum/stages` - المراحل الدراسية
- `GET /api/official-curriculum/tracks` - المسارات التعليمية
- `GET /api/official-curriculum/teacher-rank-loads` - نصاب المعلمين
- `GET /api/timetable-readiness/check` - فحص جاهزية الجدول

### صفحة الجدول المدرسي ✅ (مارس 2026)

**التحسينات:**
1. ✅ حذف أزرار "عرض الجدول" و"الجدول الكامل" من شريط الأدوات
2. ✅ نافذة نتيجة التوليد تحتوي فقط على زر "إغلاق"
3. ✅ بعد إغلاق النافذة، يتم اختيار أول فصل تلقائياً ويُعرض جدوله
4. ✅ إزالة قسم العرض الكامل المنفصل (الجدول يعرض في Schedule Grid الأصلي)

### إدارة المستخدمين والفصول ✅ (مارس 2026)

**التحسينات:**
1. ✅ نقل زر "استيراد/تصدير" من الفلاتر لشريط الأكشن
2. ✅ إصلاح dropdown الصفوف في معالج إنشاء الفصل (يعرض `name_ar`)
3. ✅ إصلاح معالجة أخطاء التحقق في CreateClassWizard

---

### التقارير والتحليلات ✅
- إلغاء كارت "متوسط الدرجات"
- بيانات ديناميكية من قاعدة البيانات

### رؤى الذكاء الاصطناعي ✅
- تحسين ألوان النص في كارت "مؤشر الأداء الذكي"

### إعدادات الحساب ✅
- إضافة قائمة الألقاب (السيد، دكتور، أستاذ...)
- زر تبديل الدور بين الحسابات

### بوابة الطالب ✅
- لوحة تحكم مع الترحيب والإحصائيات
- صفحة الجدول الدراسي
- صفحة الدرجات
- صفحة الحضور والغياب
- صفحة الواجبات

### بوابة ولي الأمر ✅
- لوحة تحكم مع قائمة الأبناء
- صفحة تفاصيل الابن
- عرض الدرجات والحضور والجدول

### تنظيف البيانات الوهمية ✅
- حذف 7 مستخدمين تجريبيين (@test.com)
- حذف 2 مدارس اختبارية
- تنظيف مجموعات demo_*

### دمج الاستيراد/التصدير في إدارة المستخدمين ✅ (مارس 2026)
- نقل وظائف صفحة BulkImportExportPage إلى صفحة UsersClassesManagement
- إضافة تبويب "استيراد/تصدير" مع تبويبات فرعية للاستيراد والتصدير
- الاستيراد: دعم الطلاب والمعلمين، تحميل القوالب، رفع الملفات، عرض النتائج
- التصدير: دعم 5 أنواع (الطلاب، المعلمين، الجدول، الحضور، الدرجات)
- إزالة الصفحة المستقلة ورابطها من القائمة الجانبية
- إزالة مسار /admin/bulk من التوجيه

### إصلاح بطاقات إحصائيات إدارة المستخدمين ✅ (مارس 2026)
- إصلاح مشكلة عرض أرقام قديمة/خاطئة في بطاقات الإحصائيات
- إزالة الحساب المحلي للإحصائيات من fetchUsers() الذي كان يُلغي بيانات الـ API
- الاعتماد بالكامل على `/api/admin/command-center/stats` للإحصائيات الحية
- جميع البطاقات تعكس الآن البيانات الحقيقية من قاعدة البيانات

### توحيد مصدر البيانات وتنظيف البيانات الوهمية ✅ (مارس 2026)
**الهدف:** جعل النظام ديناميكي 100% ويعتمد فقط على قاعدة البيانات

#### التنظيف المنجز:
1. **AIInsightsPage.jsx:** إزالة البيانات الوهمية للتوقعات والتوصيات والتنبيهات والطلاب
2. **PlatformSettingsPage.jsx:** إزالة ACTIVE_SESSIONS و VERSION_HISTORY وإضافة empty states
3. **IntegrationsPage.jsx:** تحويل الصفحة لجلب البيانات من API بدلاً من PREMIUM_INTEGRATIONS
4. **AdminDashboard.jsx:** إزالة البيانات التجريبية للرسوم البيانية للمدارس

#### تحديث قاعدة البيانات:
- تصفير أعداد الطلاب والمعلمين في جميع المدارس بناءً على الأعداد الحقيقية
- إزالة الحقول القديمة (student_count, teacher_count)

#### Empty States:
- جميع الصفحات تعرض حالات فارغة واضحة عند عدم وجود بيانات
- لا يتم عرض بيانات وهمية في أي مكان

### تثبيت الإعدادات الأساسية والهيكل الأكاديمي ✅ (مارس 2026)

#### 1. البيانات المرجعية المثبتة:
- **الهيكل الأكاديمي**: 3 مراحل (ابتدائية، متوسطة، ثانوية) + 12 صف دراسي + مساران (عام، تحفيظ)
- **المواد الدراسية**: 12 مادة مرجعية (لغتي، رياضيات، علوم، إسلامية، قرآن، إنجليزي، الخ)
- **رتب المعلمين**: 7 رتب مع النصاب التدريسي (14-24 حصة أسبوعية)
- **القيود الإدارية**: 6 قيود للجدولة (تعارض المعلم/الفصل، الحصص المتتالية، الخ)

#### 2. الإعدادات الافتراضية للمدارس:
- أيام العمل: الأحد - الخميس
- عدد الحصص: 7 حصص يومياً
- التوقيت: 07:00 - 13:15
- الفترات الزمنية: 9 فترات (7 حصص + استراحة + صلاة)
- مدة الحصة: 45 دقيقة

#### 3. بيانات الاختبار المضافة (محدّث مارس 2026):
- **مدرسة النور (SCH-001)**:
  - المدينة: الرياض، حي الياسمين
  - المدير: الأستاذ طلال أحمد (principal1@nassaq.com)
  - 25 طالب، 5 معلمين، 3 فصول (1A، 2A، 7A)
  - المعلمون: أحمد عبد الرحمن، منى علي، خالد السبيعي، سارة محمد، عبد الله الحربي
  
- **مدرسة الأحساء (SCH-002)**:
  - المدينة: الأحساء، الهفوف
  - المدير: الأستاذ محمد عبد العزيز (principal4@nassaq.com)
  - 25 طالب، 5 معلمين، 3 فصول (1A، 2A، 7A)
  - المعلمون: يوسف عادل، هبة عبد العزيز، إبراهيم حسن، آلاء كمال، محمد جابر

- **إجمالي**: 50 طالب، 10 معلمين، 6 فصول

#### 4. استثناءات التوافر والقيود الإدارية:
- أحمد عبد الرحمن: غير متاح الحصة الأولى يوم الأحد
- سارة محمد: غير متاحة الحصة السابعة يوم الأربعاء
- يوسف عادل: غير متاح الحصة الأولى يوم الإثنين
- آلاء كمال: غير متاحة الحصتين الأولى والثانية يوم الثلاثاء

#### 5. APIs الجديدة:
- `GET /api/reference/academic-structure` - الهيكل الأكاديمي الكامل
- `GET /api/reference/subjects` - المواد المرجعية
- `GET /api/reference/teacher-ranks` - رتب المعلمين
- `GET /api/reference/admin-constraints` - القيود الإدارية
- `GET /api/reference/default-settings` - الإعدادات الافتراضية
- `GET /api/school/settings` - إعدادات المدرسة (محدث)

### إصلاحات API (مارس 2026):
- تحويل أسماء الحقول (full_name_ar → full_name) في APIs المعلمين والطلاب
- تحويل أسماء الفصول (name_ar → name) في API الفصول
- إصلاح مسارات API المزدوجة في SchoolSettingsPage.jsx

### المنهج الرسمي الكامل - Official Curriculum Data ✅ (ديسمبر 2026)

#### البيانات المُثبتة في قاعدة البيانات:
تم تعبئة قاعدة البيانات ببيانات المنهج الرسمي الكامل من وزارة التعليم السعودية:

**1. المراحل الدراسية (3):**
- المرحلة الابتدائية (6 صفوف)
- المرحلة المتوسطة (3 صفوف)
- المرحلة الثانوية (3 صفوف)

**2. المسارات التعليمية (8):**
- التعليم العام
- تحفيظ القرآن الكريم
- السنة الأولى المشتركة
- المسار العام
- مسار علوم الحاسب والهندسة
- مسار الصحة والحياة
- مسار إدارة الأعمال
- المسار الشرعي

**3. الصفوف والسنوات (29):**
- 12 صف للتعليم العام والتحفيظ (ابتدائي + متوسط)
- 17 سنة للمرحلة الثانوية بمختلف المسارات

**4. المواد الدراسية (81):**
جميع المواد الرسمية مصنفة حسب الفئة:
- islamic, language, science, social, technology, engineering, health, business, law, skills, physical, arts, activity, non_class, project, optional

**5. توزيع المواد (394):**
توزيع كامل للحصص السنوية والأسبوعية لكل مادة في كل صف

**6. نصاب المعلمين (7 رتب):**
| الرتبة | النصاب الأسبوعي |
|--------|----------------|
| المعلم | 24 حصة |
| المعلم الممارس | 24 حصة |
| المعلم المتقدم | 22 حصة |
| المعلم الخبير | 18 حصة |
| معلم تربية خاصة - ممارس | 18 حصة |
| معلم تربية خاصة - متقدم | 16 حصة |
| معلم تربية خاصة - خبير | 14 حصة |

#### APIs المنهج الرسمي الجديدة:
```
GET /api/official-curriculum/stats           - إحصائيات المنهج
GET /api/official-curriculum/stages          - المراحل الدراسية
GET /api/official-curriculum/tracks          - المسارات التعليمية
GET /api/official-curriculum/grades          - الصفوف والسنوات
GET /api/official-curriculum/subjects        - المواد الدراسية
GET /api/official-curriculum/subject-categories - تصنيفات المواد
GET /api/official-curriculum/grade-subjects/{grade_id} - مواد صف محدد
GET /api/official-curriculum/teacher-rank-loads - نصاب المعلمين
GET /api/official-curriculum/stage/{stage_id}/full - المنهج الكامل لمرحلة
GET /api/official-curriculum/complete        - المنهج الكامل
```

#### تحديثات واجهة المستخدم:
- **Accordion تفاعلي لتوزيع المواد:** عرض هرمي للمراحل > المسارات > الصفوف > المواد
- **جدول مواد تفصيلي:** يعرض المادة، الحصص السنوية، الأسبوعية، النوع، الحالة
- **مؤشرات القفل:** شارات "رسمي" و"للقراءة فقط" على جميع البيانات الثابتة
- **رسالة تحذيرية:** "هذه البيانات رسمية ومقفلة - لا يمكن تعديلها"

#### Collections الجديدة في MongoDB:
- `official_curriculum_stages`
- `official_curriculum_tracks`
- `official_curriculum_grades`
- `official_curriculum_subjects`
- `official_curriculum_subject_details`
- `official_teacher_rank_loads`

---

### إصلاحات حفظ البيانات (ديسمبر 2026)

#### المشاكل المُصلحة:
1. **مشكلة حفظ بيانات الطلاب والفصول والمعلمين:**
   - **السبب:** APIs التعديل كانت تستخدم models تتطلب كل الحقول
   - **الحل:** أنشأت `StudentUpdate`, `ClassUpdate`, `TeacherUpdate` بحقول اختيارية

2. **خطأ "Objects are not valid as React child":**
   - **السبب:** الأخطاء من Backend كانت تُعرض مباشرة ككائنات
   - **الحل:** إضافة معالجة للأخطاء لتحويلها إلى نصوص

3. **مسارات API خاطئة في صفحة الإعدادات:**
   - **السبب:** استخدام مسارات غير موجودة (`/school/teachers`)
   - **الحل:** تصحيح المسارات إلى (`/teachers`, `/classes`, `/teacher-assignments`)

#### الملفات المُعدلة:
- `backend/server.py` - إضافة models للتحديث الجزئي وإصلاح APIs
- `frontend/src/pages/UsersClassesManagement.jsx` - معالجة الأخطاء
- `frontend/src/pages/SchoolSettingsPagePro.jsx` - تصحيح مسارات API

---

### تحديثات واجهة إعدادات المدرسة (ديسمبر 2026)

#### 1. إعادة تصميم إسناد المعلمين بـ Drag & Drop:
- **قائمة المواد:** 81 مادة متاحة للسحب والإفلات
- **صناديق المعلمين:** كل معلم له صندوق خاص لاستقبال المواد
- **المواد المسندة:** تظهر داخل صندوق المعلم مع زر إلغاء
- **الحفظ التلقائي:** يتم حفظ الإسناد فوراً في قاعدة البيانات

#### 2. إزالة أزرار الإضافة:
- **حذف زر "إضافة معلم"** من تبويب إسناد المعلمين
- **حذف زر "إضافة فصل"** من تبويب الفصول والشعب
- **البيانات تُعرض فقط** من قاعدة البيانات الرئيسية

#### 3. تحديث الألوان لتتناسب مع الهوية:
- **قسم البيانات الثابتة:** من الأخضر إلى الفيروزي (brand-turquoise)
- **أيام العمل النشطة:** الأزرق الداكن (brand-navy)
- **التبويبات:** متسقة مع الهوية البصرية

#### 4. إصلاح خطأ البريد الإلكتروني:
- **السبب:** إرسال email فارغ أو غير صالح
- **الحل:** إرسال email فقط إذا كان صالحاً (يحتوي على @)

#### 5. إصلاح خطأ إسناد المادة:
- **السبب:** `class_id` كان مطلوباً
- **الحل:** جعل `class_id` اختياري للإسنادات العامة

---

## حسابات الاختبار

| الدور | البريد الإلكتروني | كلمة المرور |
|-------|------------------|-------------|
| مدير المنصة | admin@nassaq.com | Admin@123 |
| مدير مدرسة النور | principal1@nassaq.com | Principal@123 |
| مدير مدرسة الأحساء | principal4@nassaq.com | Principal@123 |
| معلم (النور) | teacher1@nor.edu.sa | Teacher@123 |
| معلم (الأحساء) | teacher1@ahsa.edu.sa | Teacher@123 |
| طالب (النور) | student1@nor.edu.sa | Student@123 |
| طالب (الأحساء) | student1@ahsa.edu.sa | Student@123 |

---

## محرك الجدولة الذكي ✅ (مارس 2026)

### الملخص
تم تنفيذ محرك جدولة ذكي كامل يستخدم خوارزميات متقدمة لتوليد الجداول الدراسية تلقائياً.

### الملفات الرئيسية
- `/app/backend/engines/smart_scheduling_engine.py` - المحرك الأساسي (1500+ سطر)
- `/app/backend/server.py` - APIs الجدولة الذكية

### مراحل التوليد (12 مرحلة)
1. **التحقق من جاهزية البيانات** - فحص المدرسة، العام الدراسي، الصفوف، المعلمين، الإعدادات
2. **تحميل إعدادات المدرسة** - أيام العمل، الحصص، الأوقات، الفترات
3. **بناء مصفوفة الطلب الأكاديمي** - الفصول والمواد وعدد الحصص المطلوبة
4. **بناء مصفوفة الموارد المتاحة** - المعلمين وتوافرهم ونصابهم
5. **التحقق المسبق من التعارضات** - فحص السعة والتغطية
6. **توليد مسودة الجدول** - توزيع الحصص على الفترات
7. **اكتشاف التعارضات** - تحديد المشاكل في الجدول
8. **تحسين الجدول** - تقليل التعارضات وموازنة الأحمال

### APIs الجديدة
| API | الوصف |
|-----|-------|
| `GET /smart-scheduling/validate/{school_id}` | التحقق من جاهزية البيانات |
| `POST /smart-scheduling/generate/{school_id}` | توليد جدول جديد |
| `GET /smart-scheduling/timetables/{school_id}` | عرض جداول المدرسة |
| `GET /smart-scheduling/timetable/{id}` | تفاصيل جدول |
| `GET /smart-scheduling/timetable/{id}/sessions` | حصص الجدول |
| `GET /smart-scheduling/timetable/{id}/conflicts` | تعارضات الجدول |
| `POST /smart-scheduling/timetable/{id}/publish` | نشر الجدول |
| `POST /smart-scheduling/timetable/{id}/archive` | أرشفة الجدول |
| `DELETE /smart-scheduling/timetable/{id}` | حذف الجدول |
| `GET /smart-scheduling/pre-check/{school_id}` | فحص قابلية الجدولة |
| `GET /smart-scheduling/demand-matrix/{school_id}` | مصفوفة الطلب |
| `GET /smart-scheduling/resource-matrix/{school_id}` | مصفوفة الموارد |
| `GET /smart-scheduling/run/{id}/logs` | سجلات التشغيل |

### خوارزمية التوليد
```
1. ترتيب الطلبات حسب الصعوبة (الأصعب أولاً)
2. لكل طلب حصة:
   - توليد المرشحين الصالحين
   - حساب درجة كل مرشح
   - اختيار أفضل مرشح
   - تسجيل الإسناد
3. اكتشاف التعارضات
4. تحسين التوزيع
```

### النتائج
- **60 حصة** تم جدولتها بنجاح لـ 3 فصول
- **0 تعارضات** بعد الإصلاحات
- **100% نسبة التحسين**

### التكامل مع الواجهة
- تحديث `SchedulePageNew.jsx` لاستخدام APIs الجديدة
- دعم عرض الجداول الذكية
- تحديث تلقائي بعد التوليد

---

## صفحة الجدول الشامل ✅ (مارس 2026)

### الملخص
تم إنشاء صفحة جديدة لعرض الجدول المدرسي الشامل مع جميع الفصول والمعلمين.

### الملفات الجديدة
- `/app/frontend/src/pages/SmartTimetablePage.jsx` - صفحة الجدول الشامل

### الميزات المنفذة
1. **عرض شامل لجميع الفصول** - كل فصل بجدوله الخاص
2. **عرض جدول المعلم** - جدول كل معلم منفصل
3. **ثلاثة أوضاع عرض:**
   - الكل (جميع الفصول)
   - فصل محدد
   - معلم محدد
4. **تصدير الجدول:**
   - CSV للفتح في Excel
   - PDF/طباعة
5. **إحصائيات:**
   - عدد الحصص المجدولة
   - عدد الفصول
   - عدد المعلمين
   - عدد التعارضات
6. **إدارة التعارضات** - عرض التعارضات مع تفاصيلها
7. **نصاب المعلمين** - عرض توزيع الحصص على المعلمين

### التحديثات على صفحة الجدول الحالية
- إزالة نافذة "نتيجة معالجة الجدول" بعد التوليد
- إضافة زر "عرض الجدول" للانتقال للصفحة الجديدة
- نافذة حالة التنفيذ بعد إتمام الجدول مع زر "عرض الجدول الكامل"

### المسارات الجديدة
- `/admin/smart-timetable`
- `/school/smart-timetable`

---

## التعديل اليدوي على الحصص ✅ (مارس 2026)

### الملخص
تم إضافة إمكانية التعديل اليدوي على حصص الجدول الذكي.

### APIs الجديدة
| API | الوصف |
|-----|-------|
| `PUT /smart-scheduling/session/{id}` | تعديل حصة |
| `PUT /smart-scheduling/session/{id}/force` | تعديل بالقوة (تجاهل التعارضات) |
| `POST /smart-scheduling/sessions/swap` | تبديل حصتين |
| `DELETE /smart-scheduling/session/{id}` | حذف حصة |
| `POST /smart-scheduling/session/add` | إضافة حصة يدوية |

### الميزات
1. **وضع التعديل** - تفعيل/إلغاء من شريط الأدوات
2. **تعديل المعلم** - تغيير المعلم لأي حصة
3. **تعديل الوقت** - نقل الحصة ليوم/حصة أخرى
4. **حذف حصة** - حذف حصة من الجدول
5. **فحص التعارضات** - تحذير عند وجود تعارض

---

## واجبات الطالب ✅ (مارس 2026)

### الملخص
تم إضافة نظام الواجبات المدرسية لبوابة الطالب.

### APIs الجديدة
| API | الوصف |
|-----|-------|
| `GET /student-portal/assignments` | واجبات الطالب |
| `POST /student-portal/assignments/{id}/submit` | تسليم واجب |
| `GET /student-portal/assignments/{id}` | تفاصيل الواجب |

### الملفات
- `/app/frontend/src/pages/student/StudentAssignments.jsx` - صفحة الواجبات
- `/app/backend/routes/student_portal_routes.py` - APIs الواجبات

### الميزات
1. **عرض الواجبات** - جميع الواجبات مع حالتها
2. **تسليم الواجب** - إرسال الإجابة
3. **عرض الدرجات** - درجة المعلم وملاحظاته
4. **إحصائيات** - قيد الانتظار، تم التسليم، تم التقييم
5. **تنبيهات** - تحذير قبل موعد التسليم

---

## المهام القادمة

### P1 - أولوية عالية
- [x] ~~تصدير الجداول بصيغ PDF و CSV~~ ✅
- [x] ~~تكامل الواجبات مع بوابة الطالب~~ ✅
- [x] ~~عرض الجدول المُنشأ في نفس الصفحة~~ ✅ (مارس 2026)
- [x] ~~نافذة حالة بعد اكتمال التوليد~~ ✅ (مارس 2026)
- [x] ~~إعادة تصميم بطاقة أيام العمل~~ ✅ (مارس 2026)
- [x] ~~حساب تلقائي للأوقات~~ ✅ (مارس 2026)
- [x] ~~تفعيل/تعطيل المراحل في الجدولة~~ ✅ (مارس 2026)

### P2 - أولوية متوسطة
- [x] ~~إضافة واجهة لإدارة التعارضات~~ ✅
- [x] ~~إضافة إمكانية التعديل اليدوي على الحصص~~ ✅
- [ ] تطبيق موبايل للطلاب وأولياء الأمور
- [ ] تحليلات متقدمة لوحدة التواصل
- [ ] تصدير الجداول إلى PDF مع تصميم احترافي

---

### تحسينات واجهة الجدولة وإعدادات المدرسة ✅ (مارس 2026)

#### 1. صفحة الجدولة (SchedulePageNew.jsx):
- **عرض الجدول في نفس الصفحة:** بدلاً من التنقل لصفحة جديدة، يظهر الجدول الكامل مباشرة في نفس الصفحة
- **نافذة حالة (Modal):** تظهر بعد اكتمال التوليد مع إحصائيات (عدد الحصص المجدولة، نسبة النجاح)
- **أدوات التصفية:** تبويبات "حسب الفصل" | "حسب المعلم" | "عرض شامل"
- **زر الرجوع:** للعودة من عرض الجدول الكامل للصفحة الرئيسية
- **data-testids:** `view-full-timetable-btn`, `back-from-timetable-btn`

#### 2. بطاقة أيام العمل والعطلة (الجديدة):
- تصميم بصري محسّن مع خلفية متدرجة خضراء
- دوائر إحصائية (5 أيام دراسة / 2 أيام عطلة)
- شبكة الأيام الأسبوعية ملونة (أخضر للدراسة، أحمر للعطلة)
- شارة الحالة لكل يوم

#### 3. حوار تعديل الأوقات (المحسّن):
- **إدخال مبسط:** وقت بداية الحصة الأولى فقط
- **حساب تلقائي:** زر "احسب التوزيع الزمني تلقائياً"
- **معاينة:** عرض جميع الفترات المحسوبة قبل الحفظ
- **خيارات:** عدد الحصص، مدة الحصة، مدة الاستراحة، مدة الصلاة
- **data-testid:** `auto-calculate-btn`, `first-period-start-time`

#### 4. تفعيل/تعطيل المراحل:
- زر Toggle لكل مرحلة في تبويب "الهيكل الأكاديمي"
- المراحل المُعطّلة تظهر باهتة ولا تُدرج في الجدولة
- رسالة توضيحية للمستخدم
- **data-testid:** `toggle-stage-{id}`

#### 5. إصلاح تسجيل الدخول:
- school_principal يتوجه لـ `/principal` بدلاً من `/school`
- تحسين جلب الأدوار مع timeout لتجنب race conditions

---

## البنية التقنية

```
/app
├── backend/
│   ├── routes/
│   │   ├── student_portal_routes.py
│   │   ├── parent_portal_routes.py
│   │   └── ...
│   └── server.py
└── frontend/
    └── src/
        ├── pages/
        │   ├── StudentPortal/
        │   │   ├── StudentPortalDashboard.jsx
        │   │   ├── StudentSchedulePage.jsx
        │   │   ├── StudentGradesPage.jsx
        │   │   ├── StudentAttendancePage.jsx
        │   │   └── StudentHomeworkPage.jsx
        │   ├── ParentPortal/
        │   │   ├── ParentPortalDashboard.jsx
        │   │   └── ChildDetailsPage.jsx
        │   └── ...
        └── components/
            └── portal/
                └── PortalLayout.jsx
```

---

### إعادة تصميم صفحة إعدادات المدرسة ✅ (مارس 2026)

#### التصميم الجديد الاحترافي:
تم إنشاء صفحة `SchoolSettingsPagePro.jsx` بتصميم احترافي وبصري وذكي يتضمن 8 تبويبات:

1. **نظرة عامة (Overview):**
   - عرض معلومات المدرسة والإحصائيات (المعلمين، الفصول، المواد، الحصص)
   - أيام العمل والعطلة بشكل بصري (5 أيام دراسة، 2 أيام عطلة)
   - اليوم الدراسي (07:00 - 13:15)
   - كروت إحصائيات سريعة قابلة للنقر

2. **التوقيت (Timing):**
   - التوزيع الزمني الكامل (9 فترات)
   - 7 حصص دراسية (45 دقيقة لكل حصة)
   - استراحة (20 دقيقة)
   - فترة صلاة (20 دقيقة)

3. **المعلمون (Teachers):**
   - عرض قائمة المعلمين مع تخصصاتهم
   - النصاب الأسبوعي لكل معلم
   - ❌ تم إزالة زر "إضافة معلم" (الإضافة من صفحة إدارة المستخدمين)

4. **الفصول (Classes):**
   - عرض الفصول مع عدد الطلاب
   - الصف الدراسي والشعبة
   - ❌ تم إزالة زر "إضافة فصل" (الإضافة من صفحة إدارة المستخدمين)

5. **المواد (Subjects):**
   - 12 مادة دراسية مرجعية
   - عدد الحصص الأسبوعية لكل مادة
   - ألوان مميزة حسب تصنيف المادة

6. **الرتب (Ranks):**
   - 7 رتب للمعلمين حسب نظام وزارة التعليم
   - النصاب التدريسي (14-24 حصة أسبوعية)
   - الحد اليومي الأقصى للحصص

7. **القيود (Constraints):**
   - 6 قيود إدارية للجدولة
   - مستويات الأولوية (حرج، عالي، متوسط)
   - حالة التفعيل لكل قيد

8. **الهيكل (Structure):**
   - 3 مراحل دراسية (ابتدائية، متوسطة، ثانوية)
   - 12 صف دراسي مع القابلية للتوسيع
   - مساران تعليميان (التعليم العام، تحفيظ القرآن)

#### APIs المُحدّثة:
- `GET /api/reference/stages` - المراحل الدراسية
- `GET /api/reference/grades` - الصفوف الدراسية
- `GET /api/reference/tracks` - المسارات التعليمية
- `GET /api/reference/subjects` - المواد المرجعية
- `GET /api/reference/teacher-ranks` - رتب المعلمين
- `GET /api/reference/admin-constraints` - القيود الإدارية
- `GET /api/reference/default-settings` - الإعدادات الافتراضية

#### نتائج الاختبار:
- Backend: 15/15 اختبار نجح (100%)
- Frontend: جميع 8 تبويبات تعمل بشكل صحيح

---

### إعادة تصميم صفحة الجدول المدرسي ✅ (مارس 2026)

#### التصميم الجديد الاحترافي:
تم إنشاء صفحة `SchedulePageNew.jsx` بتصميم مرئي واضح وبصري:

**الميزات الرئيسية:**
1. **عرضان للجدول:**
   - **بالفصل:** عرض الجدول الأسبوعي لفصل محدد
   - **بالمعلم:** عرض الجدول الأسبوعي لمعلم محدد

2. **التصميم المرئي:**
   - ألوان مختلفة لكل مادة (لغتي=أخضر، رياضيات=أزرق، علوم=بنفسجي، إنجليزي=أحمر، إسلامية=عنبر)
   - فترات الاستراحة والصلاة تظهر بأيقونات واضحة
   - كارت لكل حصة يعرض: المادة، المعلم، الفصل

3. **الإحصائيات:**
   - شارة حالة الجدول (مسودة/منشور)
   - عدد الحصص الكلي
   - زر معالجة الجدول بالذكاء الاصطناعي

4. **التفاعل:**
   - النقر على أي حصة يفتح تفاصيلها
   - التبديل بين العرض بالفصل/المعلم
   - دليل ألوان المواد

#### إصلاح Backend API:
- إصلاح `GET /api/schedule-sessions` ليرجع أسماء المعلمين والمواد من `teacher_assignments`
- الآن يعرض 35 حصة مع البيانات الكاملة

#### نتائج الاختبار:
- Backend: 12/12 اختبار نجح (100%)
- Frontend: جميع الميزات تعمل بشكل صحيح

---

## آخر تحديث
12 مارس 2026 - تحسينات شاملة على صفحة الجدول:

### التحسينات الجديدة:
1. **زر "نشر الجدول":**
   - Dialog يوضح ما سيراه كل مستخدم (المعلمون، الطلاب، أولياء الأمور)
   - ينشر الجدول ليكون متاحاً لجميع المستخدمين

2. **زر "الجدول الكامل":**
   - عرض الجدول الأسبوعي الكامل لجميع الفصول

3. **Dialog معالجة الجدول بالذكاء الاصطناعي المحسّن:**
   - عرض الخطوات التي سيتم تنفيذها
   - الهدف: نسبة نجاح 100%
   - عرض حالة المعالجة أثناء التنفيذ
   - خيارات لمعالجة الحصص الغير مجدولة

4. **Dialog تعديل مواعيد اليوم الدراسي:**
   - تعديل بداية ونهاية الدوام
   - تعديل عدد الحصص ومدتها
   - تعديل مدة الاستراحة والصلاة
   - توليد تلقائي للتوزيع الزمني

5. **أزرار تفعيل/تعطيل القيود الإدارية:**
   - تبديل حالة كل قيد بنقرة واحدة

6. **إزالة "عرض التفاصيل" من كروت KPI:**
   - تبسيط واجهة مركز القيادة

### الملفات المُحدّثة:
- `/app/frontend/src/pages/SchedulePageNew.jsx`
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx`
- `/app/frontend/src/components/dashboard/SchoolDashboardContent.jsx`
- `/app/backend/server.py` (APIs جديدة)

### المهام المتبقية:
- ~~تعديل المواد الدراسية من صفحة الإعدادات~~ ✅
- ~~إضافة قيود إدارية جديدة~~ ✅
- حساب نسبة الحضور من قاعدة البيانات
- تحديث صفحة التقارير (Dynamic Data)
- رفع صورة الملف الشخصي
- تحسين صفحة رؤى الذكاء الاصطناعي

---

## تحديث 12 مارس 2026 - المهام ذات الأولوية القصوى

### ✅ المهام المكتملة:

#### 1. إصلاح مشكلة "الفترات الزمنية غير معرّفة"
- تم التحقق من أن الفترات الزمنية تعمل بشكل صحيح
- صفحة الجدول تعرض 7 حصص + استراحة + فترة صلاة
- الأوقات: 07:00 - 13:15

#### 2. CRUD المواد الدراسية (إضافة/تعديل/حذف)
- **API جديدة:** `POST /api/school/subjects` - إضافة مادة
- **API جديدة:** `PUT /api/school/subjects/{id}` - تعديل مادة
- **API جديدة:** `DELETE /api/school/subjects/{id}` - حذف مادة (soft delete)
- **API جديدة:** `GET /api/school/subjects` - جلب المواد
- **واجهة المستخدم:** زر "إضافة مادة" + Dialog للإضافة والتعديل
- **واجهة المستخدم:** أزرار التعديل والحذف لكل مادة

#### 3. CRUD القيود الإدارية (إضافة/تعديل/حذف)
- **API جديدة:** `POST /api/school/constraints` - إضافة قيد
- **API جديدة:** `PUT /api/school/constraints/{id}` - تعديل قيد
- **API جديدة:** `DELETE /api/school/constraints/{id}` - حذف قيد (soft delete)
- **API جديدة:** `GET /api/school/constraints` - جلب القيود
- **واجهة المستخدم:** زر "إضافة قيد" + Dialog للإضافة والتعديل
- **واجهة المستخدم:** أزرار التعديل والحذف لكل قيد
- **واجهة المستخدم:** زر تفعيل/تعطيل لكل قيد

#### 4. Active Role Context (سياق الدور النشط)
- **API جديدة:** `GET /api/auth/active-role` - جلب سياق الدور النشط
- **API جديدة:** `POST /api/auth/set-active-role` - تعيين سياق الدور النشط
- **البيانات المحفوظة:** User Identity ID, Role ID, School ID
- **الاستجابة:** اسم الدور بالعربية، اسم المدرسة، وقت التعيين

#### 5. إسنادات المعلمين والفصول
- **تبويب جديد:** "الإسنادات" في صفحة إعدادات المدرسة
- **عرض الإسنادات:** مجمعة حسب المعلم مع الفصل والمادة وعدد الحصص
- **CRUD كامل:** إضافة/تعديل/حذف الإسنادات
- **إصلاح توليد الجدول:** يبحث عن الإسنادات بدون تصفية السنة إذا لم يجد

#### 6. إصلاح قوائم نموذج إضافة معلم
- **قائمة الجنسيات:** تعمل الآن (سعودي، مصري، أردني، سوري، إلخ)
- **قائمة الدرجة العلمية:** تعمل الآن (دبلوم، بكالوريوس، ماجستير، دكتوراه)
- **قائمة الرتبة الوظيفية:** تعمل الآن (المعلم، المعلم الممارس، المتقدم، الخبير، إلخ)
- **قائمة نوع العقد:** تعمل الآن

#### 7. إصلاح نموذج إضافة طالب ✅ (مارس 2026 - P0 FIXED)
- **المشكلة:** كان إنشاء طالب جديد يفشل مع رسالة خطأ عامة
- **السبب:** props مفقودة (`api`, `grades`, `classes`) لم يتم تمريرها للـ wizard في صفحة UsersClassesManagement و SchoolSettingsPage
- **الإصلاحات:**
  1. إضافة `grades` state وجلبها من API في `UsersClassesManagement.jsx`
  2. تمرير `api`, `grades`, `classes` إلى `AddStudentWizard` في كل الصفحات
  3. إصلاح عرض أسماء الصفوف باستخدام `name_ar`/`name_en` بدلاً من `name` فقط
- **نتائج الاختبار:** 100% Backend + 100% Frontend
- تقرير الاختبار: `/app/test_reports/iteration_67.json`

#### 8. إصلاح Dashboard حضور المعلمين
- يجلب الحضور من `teacher_attendance` collection
- يدعم fallback إلى `attendance` collection

### نتائج الاختبار:
- **Backend APIs:** جميعها تعمل ✅
- **Frontend:** جميع القوائم تعرض البيانات ✅
- **Student Wizard:** 14/14 API tests passed ✅

### الملفات المُحدّثة:
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx` - تبويب الإسنادات
- `/app/frontend/src/components/wizards/AddTeacherWizard.jsx` - إصلاح القوائم
- `/app/frontend/src/components/wizards/AddStudentWizard.jsx` - إصلاح عرض الصفوف (name_ar/name_en)
- `/app/frontend/src/pages/TeachersPage.jsx` - تمرير api و grades و classes
- `/app/frontend/src/pages/UsersClassesManagement.jsx` - إضافة grades state + جلبها من API + تمريرها للـ wizard
- `/app/frontend/src/pages/SchoolSettingsPage.jsx` - تمرير api, grades, classes للـ wizard
- `/app/frontend/src/components/dashboard/SchoolDashboardContent.jsx` - جلب grades و classes
- `/app/backend/server.py` - إصلاح APIs
- `/app/backend/tests/test_student_wizard.py` - اختبارات جديدة للـ student wizard

---

## المهام القادمة (P1/P2):

### P1 - أولوية عالية:
1. **تحسين نسبة نجاح توليد الجدول:** عرض تحليل السعة (Required vs Available slots) + اقتراحات عملية
2. حساب نسبة الحضور اليومي من قاعدة البيانات
3. تحديث صفحة التقارير (Dynamic Data)

### P2 - أولوية متوسطة:
4. رفع صورة الملف الشخصي
5. تحديث بيانات المستخدم (الاسم، الهاتف، اللغة)
6. أيقونة المخ في صفحة رؤى الذكاء الاصطناعي
7. القائمة الجانبية لمدير المنصة (الشعار، الاسم، الدور)

---

## تحديثات صفحة إعدادات المدرسة ✅ (مارس 2026)

### الميزات المُنفذة:

#### 1. تعديل معلومات المدرسة الأساسية ✅
- Dialog لتعديل: اسم المدرسة (عربي/إنجليزي)، المدينة، العنوان، الهاتف، البريد
- API: `PUT /school/settings/info` مع Audit Logging
- الحقول تُحمل من قاعدة البيانات وتُحفظ فوراً

#### 2. CRUD للمراحل التعليمية ✅
- إضافة مرحلة جديدة: `POST /school/settings/stages`
- تعديل مرحلة: `PUT /school/settings/stages/{id}`
- حذف مرحلة: `DELETE /school/settings/stages/{id}` مع التحقق من التبعيات
- واجهة تفاعلية مع أزرار تعديل/حذف لكل مرحلة

#### 3. CRUD للصفوف الدراسية ✅
- إضافة صف جديد: `POST /school/settings/grades`
- تعديل صف: `PUT /school/settings/grades/{id}`
- حذف صف: `DELETE /school/settings/grades/{id}` مع التحقق من التبعيات
- اختيار المرحلة التابع لها الصف

#### 4. تعديل أيام العمل ✅
- Dialog تفاعلي للتبديل بين أيام الدراسة والعطلة
- حفظ فعلي في قاعدة البيانات: `PUT /school/settings/work-days`
- عرض ملخص أيام الدراسة والعطلة
- Audit Logging للتغييرات

#### 5. إصلاح مصدر القيود الإدارية ✅
- القيود الآن تُقرأ من `school_constraints` (خاصة بالمدرسة)
- عند أول طلب، يتم نسخ القيود المرجعية إلى قيود المدرسة
- كل مدرسة لها قيودها الخاصة المنفصلة

#### 6. عرض آخر وقت مزامنة ✅
- يظهر تحت اسم المدرسة: "آخر مزامنة: [التاريخ والوقت]"
- يُحدّث مع كل عملية حفظ

#### 7. Audit Logging ✅
- تسجيل جميع العمليات: create, update, delete
- API لجلب السجلات: `GET /school/settings/audit-logs`
- يتضمن: نوع العملية، الكيان، البيانات القديمة/الجديدة، المستخدم، الوقت

#### 8. التحقق من التبعيات قبل الحذف ✅
- عند حذف مادة مرتبطة بإسنادات: يظهر تحذير
- عند حذف مرحلة تحتوي صفوف: يُمنع الحذف
- عند حذف صف يحتوي فصول: يُمنع الحذف
- Dialog تأكيد مع عرض التبعيات

### APIs المُضافة/المُحدّثة:
- `PUT /school/settings/info` - تحديث معلومات المدرسة + Audit
- `PUT /school/settings/work-days` - تحديث أيام العمل + Audit
- `PUT /school/settings/stages/{id}` - تحديث مرحلة + Audit
- `PUT /school/settings/grades/{id}` - تحديث صف + Audit
- `DELETE /school/subjects/{id}?force=` - حذف مع تحقق التبعيات
- `GET /school/settings/audit-logs` - جلب سجلات التدقيق
- `GET /school/constraints` - جلب قيود المدرسة (من school_constraints)

### الملفات المُحدّثة:
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx` - واجهات CRUD كاملة
- `/app/backend/server.py` - APIs + Audit Logging + Dependency checks



---

## التحديثات - 13 مارس 2026

### 1. تحسين تصميم الجدول المدرسي ✅
- **Gradient Colors**: إضافة تدرجات ألوان جميلة لكروت الحصص
  - لغتي/العربية: تدرج أخضر فيروزي (from-emerald-400 to-teal-500)
  - الرياضيات: تدرج أزرق نيلي (from-blue-400 to-indigo-500)
  - العلوم: تدرج بنفسجي (from-purple-400 to-violet-500)
  - التربية الفنية: تدرج وردي (from-fuchsia-400 to-pink-500)
  - الاستراحة: تدرج أصفر برتقالي
- **تأثيرات بصرية**: ظل، hover scale، backdrop blur
- **قراءة أفضل**: نص أبيض على خلفيات ملونة

### 2. إصلاح خطأ إنشاء الفصل "Field Required" ✅
- **المشكلة**: الـ frontend يرسل `name_ar` بينما الـ backend يتوقع `name`
- **الحل**: تحديث `ClassWizardCreate` model في الـ backend:
  - جعل `name` اختياري
  - إضافة دعم `name_ar`
  - جعل `grade` و `section` اختياريين مع قيم افتراضية
  - استخراج رقم الصف من `grade_id` تلقائياً

### 3. تحميل الجدول الذكي تلقائياً ✅
- عند فتح صفحة الجدول، يتم اختيار آخر جدول ذكي تلقائياً
- الحصص تظهر مباشرة دون الحاجة لاختيار يدوي
- دعم `period_number` في دالة `getSessionForCell`

### 4. تنظيف قاعدة البيانات ✅
- حذف 11 مادة مكررة/اختبارية
- إضافة 10 مواد أساسية (12 مادة إجمالاً للمدرسة)
- حذف ملف `SmartTimetablePage.jsx` غير المستخدم
- إزالة routes قديمة من `App.js`

### الملفات المُحدّثة:
- `/app/frontend/src/pages/SchedulePageNew.jsx` - تصميم Gradient + auto-load
- `/app/backend/server.py` - إصلاح ClassWizardCreate model
- `/app/frontend/src/App.js` - إزالة SmartTimetablePage


---

## التحديثات - 13 مارس 2026 (الجزء 2)

### ✅ المهام المنجزة:

#### 1. توحيد المصطلحات
- استبدال "عدد الحصص الأسبوعية" بـ "النصاب الأسبوعي" في جميع الصفحات

#### 2. تعديل أيقونة المخ
- تغيير اللون إلى أبيض ناصع
- إضافة تأثير Pulse + Glow متحرك

#### 3. صفحة التقارير والتحليلات
- ربط جميع الإحصائيات بقاعدة البيانات الحقيقية
- إضافة API `/reports/school/behavior` للسلوك
- إضافة API `/reports/school/top-classes` لأفضل الفصول
- إضافة API `/reports/school/export` للتصدير
- إزالة جميع البيانات الوهمية

#### 4. تبويب الحضور
- ربط الجدول بسجلات الحضور الفعلية

#### 5. تبويب السلوك
- كروت الإحصائيات (إيجابي/سلبي/تحذير/تقدير) مرتبطة بقاعدة البيانات
- قسم "ملاحظات السلوك الأخيرة" يعرض بيانات حقيقية

#### 6. صفحة مركز التواصل
- تغيير اسم "الرسائل" إلى "الرسائل المستلمة"
- إضافة نوافذ للكروت (مرسلة/مستلمة/مجدولة)
- إضافة أزرار "تعديل" و "إرسال الآن" للرسائل المجدولة
- إضافة APIs جديدة:
  - `/communication/received` - الرسائل المستلمة
  - `/communication/{id}/read` - تعيين الرسالة كمقروءة
  - `/communication/{id}/send-now` - إرسال المجدولة فوراً
  - `/communication/{id}` PUT - تعديل المجدولة
  - `/communication/templates` - إنشاء قوالب

#### 7. زر تبديل المستخدم
- إضافة الزر في صفحة الإعدادات (يظهر فقط إذا كان للمستخدم أكثر من دور)
- إضافة Dialog لعرض واختيار الأدوار
- ربط بـ APIs الموجودة في `/user-roles`

### الملفات المُحدّثة:
- `/app/backend/server.py` - APIs التقارير والسلوك
- `/app/backend/routes/communication_routes.py` - APIs مركز التواصل
- `/app/frontend/src/pages/SchoolReportsPage.jsx` - التقارير
- `/app/frontend/src/pages/CommunicationCenterPage.jsx` - مركز التواصل
- `/app/frontend/src/pages/AccountSettingsPage.jsx` - تبديل المستخدم
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx` - توحيد المصطلحات
- `/app/frontend/src/pages/AIInsightsPage.jsx` - أيقونة المخ


---

## تحديثات 13 مارس 2026 - المنهج الرسمي

### المنهج الرسمي - تبويب جديد في إعدادات المدرسة ✅

#### 1. إدخال بيانات المنهج الرسمي (Seed Data)
- **سكربت**: `/app/backend/scripts/seed_official_curriculum.py`
- **البيانات المُدخلة**:
  - 3 مراحل دراسية (ابتدائية، متوسطة، ثانوية)
  - 8 مسارات تعليمية (التعليم العام، تحفيظ القرآن، السنة المشتركة، المسار العام، علوم الحاسب، الصحة والحياة، إدارة الأعمال، المسار الشرعي)
  - 29 صف دراسي
  - 75 مادة دراسية
  - 393 توزيع مادة/صف مع الحصص السنوية
  - 7 رتب للنصاب الرسمي

#### 2. APIs جديدة للمنهج الرسمي
- **ملف**: `/app/backend/routes/official_curriculum_routes.py`
- **Endpoints**:
  - `GET /api/official-curriculum/stats` - إحصائيات المنهج
  - `GET /api/official-curriculum/stages` - المراحل الدراسية
  - `GET /api/official-curriculum/tracks` - المسارات التعليمية
  - `GET /api/official-curriculum/grades` - الصفوف الدراسية
  - `GET /api/official-curriculum/subjects` - المواد الدراسية
  - `GET /api/official-curriculum/subjects/categories` - تصنيفات المواد
  - `GET /api/official-curriculum/grade-subjects` - توزيع المواد على الصفوف
  - `GET /api/official-curriculum/teacher-rank-loads` - النصاب الرسمي للمعلمين
  - `GET /api/official-curriculum/curriculum-for-grade/{grade_id}` - المنهج الكامل لصف معين
  - `GET /api/official-curriculum/search` - البحث في المنهج

#### 3. تبويب المنهج الرسمي في الواجهة
- **ملف**: `/app/frontend/src/pages/SchoolSettingsPagePro.jsx`
- **الميزات**:
  - كارت إحصائيات شامل (3 مراحل، 8 مسارات، 29 صف، 75 مادة، 393 توزيع، 7 رتب)
  - شارة "بيانات مقفلة" لتوضيح أن البيانات للقراءة فقط
  - قائمة المراحل الدراسية الرسمية (قابلة للاختيار)
  - قائمة المسارات التعليمية الرسمية (قابلة للاختيار)
  - عرض الصفوف عند اختيار مرحلة أو مسار
  - جدول المنهج الكامل عند اختيار صف (المادة، التصنيف، أساسية/اختيارية، حصص/سنة، حصص/أسبوع)
  - قسم النصاب الرسمي للمعلمين حسب الرتبة (تعليم عام + تربية خاصة)
  - ملاحظة توضيحية حول طبيعة البيانات الرسمية

#### 4. Collections جديدة في MongoDB
- `official_curriculum_stages` - المراحل الدراسية
- `official_curriculum_tracks` - المسارات التعليمية
- `official_curriculum_grades` - الصفوف الدراسية
- `official_curriculum_subjects` - المواد الدراسية
- `official_curriculum_grade_subjects` - توزيع المواد على الصفوف
- `official_teacher_rank_loads` - النصاب الرسمي للمعلمين

### الملفات المُحدّثة/المُنشأة:
- `/app/backend/scripts/seed_official_curriculum.py` - سكربت إدخال البيانات
- `/app/backend/routes/official_curriculum_routes.py` - APIs المنهج الرسمي
- `/app/backend/server.py` - تسجيل الـ router الجديد
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx` - تبويب المنهج الرسمي

### المهام القادمة (P0):
1. ~~إدخال بيانات المنهج الرسمي~~ ✅
2. ~~إنشاء APIs للمنهج الرسمي~~ ✅
3. ~~إضافة تبويب المنهج الرسمي~~ ✅
4. إعادة هيكلة باقي إعدادات المدرسة لتستخدم البيانات الرسمية كمرجع
5. منطق التحقق من جاهزية البيانات للجدولة

### المهام المستقبلية (P1/P2):
- تصدير الجداول إلى PDF/CSV
- بوابة الطلاب/أولياء الأمور
- تطبيق موبايل


---

## تحديثات 13 مارس 2026 - إعادة هيكلة صفحة إعدادات المدرسة

### إعادة الهيكلة الكاملة ✅

#### 1. تقسيم الصفحة إلى قسمين رئيسيين

**القسم الأول (أزرق) - البيانات المتغيرة لبناء الجدول:**
- نظرة عامة
- أيام العمل (جديد - تصميم محسّن)
- التوقيت
- الفصول
- المعلمون
- الإسنادات
- القيود (جديد - نظام شامل)

**القسم الثاني (أخضر) - البيانات الأساسية الثابتة:**
- المنهج الرسمي
- الهيكل الأكاديمي

#### 2. حذف التبويبات المكررة
- ❌ تم حذف تبويب "المواد" (موجود في المنهج الرسمي)
- ❌ تم حذف تبويب "الرتب" (موجود في المنهج الرسمي)

#### 3. تبويب "أيام العمل" الجديد
- كروت تفاعلية لكل يوم
- اسم اليوم بالعربية والإنجليزية
- تمييز بصري واضح (أخضر = دراسة، أحمر = عطلة)
- ملخص إحصائي (5 أيام دراسة، 2 أيام عطلة)
- ملاحظة توضيحية عن تأثير التغيير على الجدول

#### 4. نظام القيود الجديد (Timetable Constraints System)

**Hard Constraints - القيود الإلزامية (12 قيد مقفل):**
1. لا يمكن تعيين معلم لحصتين في نفس الوقت
2. لا يمكن تعيين فصل لحصتين في نفس الوقت
3. لا يمكن تعيين أكثر من مادة لنفس الفصل في نفس الحصة
4. لا يمكن وضع حصة خارج أوقات اليوم الدراسي
5. لا يمكن وضع حصة داخل فترات الاستراحة أو الصلاة
6. لا يمكن تجاوز الحد الأقصى للحصص اليومية للمعلم
7. لا يمكن تجاوز النصاب الأسبوعي للمعلم
8. لا يمكن إسناد مادة إلى معلم غير مؤهل لها
9. يجب احترام عدد الحصص الأسبوعية المطلوبة لكل مادة
10. لا يمكن وضع حصة في فترة غير متاحة للمعلم أو الفصل
11. لا يمكن استخدام فترات زمنية معطلة أو مغلقة
12. يجب احترام متطلبات نوع الغرفة (مختبر، حاسب...)

**Soft Constraints - القيود التفضيلية (9 قيود مع أوزان):**
1. توزيع حصص المادة على أكثر من يوم (وزن: 8/10)
2. تقليل الفجوات في جدول المعلم (وزن: 7/10)
3. تقليل الفجوات في جدول الفصل (وزن: 7/10)
4. المواد الثقيلة في أول اليوم (وزن: 6/10)
5. المواد الأساسية في أول الفترات (وزن: 5/10)
6. عدم تكرار نفس المادة متتالياً (وزن: 4/10)
7. موازنة الحمل التدريسي (وزن: 5/10)
8. تفضيلات المعلم الزمنية (وزن: 3/10)
9. إبقاء فترات احتياطية (وزن: 2/10)

**Custom Constraints - القيود المخصصة:**
- قيود إضافية يضيفها مدير المدرسة
- يتم حفظها في قاعدة البيانات
- يمكن تفعيلها/تعطيلها

### الملفات المُحدّثة:
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx` - إعادة هيكلة كاملة

### نتائج الاختبار:
- iteration_75: 18/18 اختبار ناجح (100%)

### المهام القادمة (P0):
1. ربط نظام القيود بمحرك الجدول (Timetable Engine)
2. تفعيل منطق التحقق من Hard Constraints أثناء الجدولة
3. تفعيل حساب Score باستخدام Soft Constraints
4. إضافة واجهة تعديل أوزان القيود التفضيلية

### المهام المستقبلية (P1/P2):
- تصدير الجداول إلى PDF/CSV
- بوابة الطلاب/أولياء الأمور
- تطبيق موبايل


---

## تحديثات 13 مارس 2026 - School Settings Readiness Engine

### نظام التحقق من جاهزية الإعدادات ✅

#### 1. Backend APIs
- **ملف**: `/app/backend/routes/timetable_readiness_routes.py`
- **Endpoints**:
  - `GET /api/timetable-readiness/check` - فحص جاهزية كاملة مع تفاصيل
  - `GET /api/timetable-readiness/summary` - ملخص سريع للحالة

#### 2. الفئات المفحوصة (8 فئات)
| الفئة | الحد الأقصى | الوصف |
|-------|------------|-------|
| السياق الأكاديمي | 20 | العام والفصل الدراسي |
| أيام الدراسة | 15 | أيام العمل والعطلة |
| هيكل اليوم الدراسي | 25 | الحصص، المدة، التوقيت |
| الفصول الدراسية | 20 | الفصول والشعب |
| المعلمون | 15 | عدد المعلمين والنصاب |
| إسنادات المعلمين | 25 | ربط المعلمين بالمواد والفصول |
| القيود | 10 | القيود الإلزامية والتفضيلية |
| المنهج الرسمي | 10 | بيانات وزارة التعليم |

#### 3. مستويات الجاهزية
- 🔴 **NOT_READY**: مشاكل حرجة - لا يمكن إنشاء الجدول
- 🟡 **PARTIALLY_READY**: تحذيرات - يمكن المتابعة مع تحذيرات
- 🟢 **FULLY_READY**: جاهز بالكامل

#### 4. واجهة المستخدم
- كارت حالة الجاهزية مع أيقونة الحالة
- نسبة الجاهزية (%)
- قائمة المشاكل الحرجة مع أزرار "إصلاح"
- شارات الفئات (أخضر/أحمر)
- زر "معالجة الجدول بالذكاء الاصطناعي" (معطل/مفعّل حسب الجاهزية)

#### 5. سلوك النظام
- يتم تحديث حالة الجاهزية تلقائياً عند تغيير الإعدادات
- أزرار الإصلاح تنقل المستخدم مباشرة للتبويب المناسب
- لا يمكن تشغيل محرك الجدول إذا كانت الحالة NOT_READY

### نتائج الاختبار:
- iteration_76: Backend 24/24 (100%) + Frontend 7/7 (100%)

### المهام القادمة (P0):
1. ربط نظام الجاهزية بصفحة الجدول المدرسي
2. ربط نظام القيود بمحرك الجدول (Timetable Engine)
3. تفعيل منطق Hard Constraints و Soft Constraints أثناء الجدولة

### المهام المستقبلية (P1/P2):
- تصدير الجداول PDF/CSV
- بوابة الطلاب/أولياء الأمور
- تطبيق موبايل


---

## تحديثات 13 مارس 2026 - إعادة تصميم صفحة الإعدادات

### إعادة التصميم الشاملة ✅

#### التغييرات الرئيسية:
1. **تقليل حجم الملف:** من 4000+ سطر إلى ~1000 سطر
2. **تبسيط التبويبات:** من 9 إلى 7 تبويبات فقط
3. **تصميم أنيق:** Navy كلون أساسي مع مساحات أكبر
4. **Hero Card للجاهزية:** دائرة نسبة الإكمال بتصميم جذاب

#### التبويبات الجديدة:
| التبويب | الوظيفة |
|---------|---------|
| نظرة عامة | إحصائيات + إعدادات العام الدراسي |
| أيام العمل | 7 كروت تفاعلية للأيام |
| التوقيت | إعدادات الحصص والاستراحة |
| الفصول | جدول + إضافة فصل |
| المعلمون | جدول + إضافة معلم |
| الإسنادات | ربط المعلمين بالمواد |
| القيود | Hard + Soft Constraints |

#### خصائص التصميم:
- **اللون الأساسي:** Navy (#1C3D74)
- **الخطوط:** Cairo للعناوين، Tajawal للنص
- **RTL:** دعم كامل
- **Responsive:** شبكات متجاوبة

### نتائج الاختبار:
- iteration_77: 15/15 (100%)

### الملفات المُحدّثة:
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx` - إعادة كتابة كاملة



---

## تحديثات 13 مارس 2026 - إعادة بناء صفحة الجدول المدرسي

### إعادة بناء صفحة الجدول المدرسي بالكامل ✅

تم إعادة بناء صفحة "الجدول المدرسي" (`/principal/timetable`) من الصفر بناءً على مواصفات تفصيلية شاملة. 

#### المكونات الجديدة:

| المكون | الوظيفة |
|--------|---------|
| `PrincipalTimetablePage` | المكون الجذر - إدارة الحالة والبيانات |
| `TimetablePageHeader` | رأس الصفحة مع metadata chips |
| `TimetableStatusBanner` | شريط الحالة الديناميكي (5 حالات) |
| `TimetableActionBar` | شريط الإجراءات الرئيسية |
| `TimetableReadinessPanel` | لوحة جاهزية البيانات |
| `TimetableViewControls` | أدوات التحكم (Tabs, Filters, Toggles) |
| `TimetableGridSection` | شبكة الجدول التفاعلية |
| `TimetableInsightsPanel` | لوحة الرؤى والتحليلات |
| `TimetableVersionManager` | إدارة نسخ الجدول |
| `TimetableIssuesSection` | قسم المشاكل والتحذيرات |

#### الـ Modals:
| Modal | الوظيفة |
|-------|---------|
| `AITimetableGenerationModal` | بدء التوليد بالذكاء الاصطناعي |
| `PartialRegenerationModal` | إعادة معالجة جزئية |
| `PublishTimetableVersionModal` | نشر النسخة |
| `ArchiveTimetableVersionModal` | أرشفة النسخة |
| `TimetableDiagnosticsModal` | تشخيص المحرك |
| `TimetableSessionDetailsDrawer` | تفاصيل الحصة |

#### APIs الجديدة:
```
GET /api/smart-scheduling/timetable/versions - نسخ الجدول
GET /api/smart-scheduling/timetable/active/sessions - حصص الجدول النشط
POST /api/timetable/generate-smart - توليد الجدول الذكي
POST /api/timetable/publish/{version_id} - نشر النسخة
```

#### حالات شريط الحالة:
1. **لا يوجد جدول** - Empty state مع زر بدء التوليد
2. **نسخة مسودة** - Draft banner مع خيارات النشر
3. **نسخة منشورة** - Published banner مع خيار إنشاء مسودة جديدة
4. **جاري التوليد** - Processing state مع شريط تقدم
5. **فشل التوليد** - Error state مع خيار إعادة المحاولة

#### أوضاع العرض:
- **بالفصل** - جدول الفصل المحدد
- **بالمعلم** - جدول المعلم المحدد
- **بالصف** - جميع فصول الصف
- **باليوم** - جدول يوم محدد

#### خصائص التصميم:
- **ألوان المواد** - نظام ألوان تلقائي حسب اسم المادة
- **خلايا تفاعلية** - Hover tooltips و Click للتفاصيل
- **Responsive** - تصميم متجاوب للشاشات المختلفة
- **RTL** - دعم كامل للعربية

### نتائج الاختبار:
- iteration_81: Backend 10/10 (100%) + Frontend 11/11 (100%)

### الملفات الجديدة:
```
/app/frontend/src/components/timetable/
├── index.js
├── types.js
├── PrincipalTimetablePage.jsx
├── TimetablePageHeader.jsx
├── TimetableStatusBanner.jsx
├── TimetableActionBar.jsx
├── TimetableReadinessPanel.jsx
├── TimetableViewControls.jsx
├── TimetableGridSection.jsx
├── TimetableInsightsPanel.jsx
├── TimetableVersionManager.jsx
├── TimetableIssuesSection.jsx
└── TimetableModals.jsx
```

### المهام القادمة (P1):
1. التحقق من روابط "إصلاح" في بطاقة الجاهزية
2. تفعيل توليد الجدول الفعلي بعد إكمال البيانات

### المهام المستقبلية (P2):
1. حفظ القيود (Constraints)
2. تصدير الجداول PDF/CSV
3. بوابة الطالب/ولي الأمر

---

## سجل التحديثات (CHANGELOG)

### 13 مارس 2026 - إصلاح مشاكل الـ Sidebar وحفظ الإعدادات

#### المشاكل المُبلّغ عنها:
1. مسافات الشريط الجانبي (Sidebar) غير صحيحة في صفحة الجدول
2. بيانات صفحة إعدادات المدرسة لا تُحفظ
3. نسبة الجاهزية (89%) لا تظهر في صفحة الجدول

#### الإصلاحات المُنفذة:

**1. إصلاح استخدام مكون Sidebar:**
- **المشكلة:** كان يتم استخدام `Sidebar` كمكون منفصل مع props `isOpen/setIsOpen` بشكل خاطئ
- **الحل:** تعديل الاستخدام ليكون كـ wrapper component (يحيط بالمحتوى) كما صُمم أصلاً
- **الملفات المعدلة:**
  - `/app/frontend/src/components/timetable/PrincipalTimetablePage.jsx`
  - `/app/frontend/src/pages/SchoolSettingsPagePro.jsx`

**2. التأكد من عمل APIs:**
- **النتيجة:** الـ APIs تعمل بشكل صحيح:
  - `GET /api/school/settings` - يرجع البيانات الصحيحة (academicYear: 1446, currentSemester: 1)
  - `PUT /api/school/settings` - يحفظ التغييرات بنجاح
  - `GET /api/timetable-readiness/check` - يرجع percentage: 89.3%

**3. نتيجة الإصلاح:**
- ✅ الشريط الجانبي يظهر بشكل صحيح مع المسافات المناسبة
- ✅ نسبة الجاهزية 89% تظهر في صفحة الجدول
- ✅ البيانات تُحفظ وتُسترجع بشكل صحيح
- ✅ أيام العمل تظهر بشكل صحيح (الأحد-الخميس)

---

### 13 مارس 2026 - إضافة نظام إسناد المعلمين للفصول + تحسين كروت الجدول

#### الميزات الجديدة:

**1. صفحة إسناد المعلمين للفصول (Teacher Class Assignment):**
- **المسار:** `/school/teacher-class-assignments`
- **الملف:** `/app/frontend/src/pages/TeacherClassAssignmentPage.jsx`
- **الوظيفة:** ربط المعلمين بالفصول الدراسية باستخدام السحب والإفلات (Drag & Drop)
- **المميزات:**
  - قائمة المعلمين مع عدد الفصول المسندة لكل معلم
  - قائمة الفصول مع حالة الإسناد (أخضر = مسند، أصفر = بدون إسناد)
  - إحصائيات فورية (فصول مسندة / بدون إسناد)
  - زر إزالة الإسناد
  - تعليمات الاستخدام

**2. APIs جديدة لإسناد المعلمين:**
- `GET /api/teacher-class-assignments` - جلب جميع الإسنادات
- `POST /api/teacher-class-assignments` - إنشاء إسناد جديد
- `DELETE /api/teacher-class-assignments/{id}` - حذف إسناد
- `GET /api/teacher-class-assignments/classes-without-teachers` - الفصول بدون معلمين
- `GET /api/teacher-class-assignments/teacher/{teacher_id}` - إسنادات معلم محدد

**3. جدول قاعدة البيانات الجديد:**
```
teacher_class_assignments:
  - id (UUID)
  - teacher_id
  - class_id
  - school_id
  - academic_year_id
  - created_at
  - updated_at
```

**4. تحسين كارت "ملخص وتحليلات":**
- تصميم أبسط وأوضح
- جودة التوزيع (%) تظهر بشكل بارز في المنتصف
- شبكة إحصائيات منظمة (2x2)
- ألوان واضحة حسب الحالة

**5. تحسين كارت "نسخ الجدول":**
- تصميم مضغوط أكثر
- قسم الأرشيف قابل للطي/التوسيع
- أيقونات أصغر وأوضح
- عرض أقصى 3 مسودات

**6. ربط زر "إصلاح" بصفحة الإسنادات:**
- تحذير "X فصل بدون إسنادات للمعلمين" يوجه لـ `/school/teacher-class-assignments`
- تحديث ملف `/app/backend/routes/timetable_readiness_routes.py`

#### الملفات المعدلة/المضافة:
- `/app/frontend/src/pages/TeacherClassAssignmentPage.jsx` (جديد)
- `/app/frontend/src/App.js` (إضافة route)
- `/app/backend/server.py` (إضافة APIs)
- `/app/backend/routes/timetable_readiness_routes.py` (تحديث fix_link)
- `/app/frontend/src/components/timetable/TimetableInsightsPanel.jsx` (إعادة تصميم)
- `/app/frontend/src/components/timetable/TimetableVersionManager.jsx` (إعادة تصميم)
- `/app/frontend/src/components/timetable/index.js` (تحديث exports)

---

### 13 مارس 2026 - إصلاح مشاكل حفظ الإعدادات + تفعيل أزرار الاستراحات وعدم التوفر

#### المشاكل التي تم إصلاحها:

**1. مشكلة حفظ العام والفصل الدراسي وأيام العمل والتوقيت:**
- **السبب:** كان `saveAllSettings` يرسل كائن `settings` كاملاً مع `timingSettings`، مما يسبب تداخل البيانات القديمة مع الجديدة
- **الحل:** تعديل دالة `saveAllSettings` لإرسال الحقول المطلوبة فقط بدلاً من `...settings`
- **الملف:** `/app/frontend/src/pages/SchoolSettingsPagePro.jsx`

**2. مشكلة أزرار تبويب الاستراحات والصلاة:**
- **السبب:** الأزرار لم يكن لها `onClick` handlers
- **الحل:** إضافة Modal جديد لإضافة/تعديل الفترات + handlers للتعديل والحذف
- **الميزات المفعّلة:**
  - زر "إضافة فترة" يفتح Modal
  - أيقونة التعديل تفتح Modal مع البيانات الحالية
  - أيقونة الحذف تطلب تأكيد وتحذف الفترة

**3. مشكلة أزرار تبويب عدم التوفر:**
- **السبب:** الأزرار لم يكن لها `onClick` handlers
- **الحل:** إضافة Modal جديد لإضافة فترات عدم التوفر للمعلمين والفصول
- **الميزات المفعّلة:**
  - زر "إضافة" في كارد المعلمين يفتح Modal
  - زر "إضافة" في كارد الفصول يفتح Modal
  - زر الحذف يحذف الفترة

**4. تحديث كارت "بيانات الجدول جاهزة جزئياً":**
- تغيير التدرج اللوني ليستخدم Brand Colors (navy -> turquoise)
- تغيير زر "إنشاء الجدول" إلى "الذهاب للجدول" مع توجيه لـ `/principal/timetable`

**5. إضافة رابط لصفحة إسناد المعلمين للفصول:**
- في تبويب "إسناد المعلمين" تم إضافة بانر أصفر مع زر "فتح صفحة الإسناد"
- يوجه المستخدم لصفحة `/school/teacher-class-assignments`

#### الملفات المعدلة:
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx`
  - إصلاح دالة `saveAllSettings`
  - إضافة Modal states: `showBreakModal`, `showUnavailabilityModal`, `editingBreak`, `unavailabilityType`
  - إضافة handlers: `handleAddBreak`, `handleEditBreak`, `handleDeleteBreak`, `handleSaveBreak`, `handleAddUnavailability`, `handleSaveUnavailability`, `handleDeleteUnavailability`
  - إضافة Modals للاستراحات وعدم التوفر
  - تحديث تصميم كارت الجاهزية

---

### 13 مارس 2026 - إصلاح توليد الجدول + تصميم التبويبات + دمج إسناد الفصول

#### الإصلاحات والتحسينات:

**1. إصلاح خطأ "Not Found" عند توليد الجدول:**
- **المشكلة:** الـ API `/api/timetable/generate-smart` غير موجود
- **الحل:** إضافة endpoint جديد في `server.py` يستدعي `smart_scheduling_engine.generate_timetable`
- **النتيجة:** زر "معالجة الجدول بالذكاء الاصطناعي" يعمل الآن بنجاح ✅

**2. تحسين تصميم التبويبات:**
- **التغيير:** التبويبات الآن في صف واحد (grid 8 columns) بحجم مناسب
- **التصميم:** أيقونة + نص عمودياً لكل تبويب
- **التبويبات:** العام والفصل الدراسي، أيام العمل، التوقيت والحصص، الاستراحات والصلاة، الفصول والشعب، إسناد المعلمين، عدم التوفر، القيود والتفضيلات

**3. دمج إسناد الفصول داخل تبويب "إسناد المعلمين":**
- **التصميم الجديد:** تبويب فرعي مقسم لقسمين:
  - **إسناد المواد** (بنفسجي): السحب والإفلات لربط المعلمين بالمواد
  - **إسناد الفصول** (فيروزي): اختيار سريع لإسناد الفصول للمعلمين
- **الحفظ التلقائي:** أي تغيير يُحفظ مباشرة في `teacher_class_assignments`
- **الربط مع الجدول:** تحديث `timetable_readiness_routes.py` لقراءة من `teacher_class_assignments`

**4. تحديث رابط "إصلاح" في صفحة الجدول:**
- **قبل:** يوجه لصفحة منفصلة `/school/teacher-class-assignments`
- **بعد:** يوجه لتبويب إسناد المعلمين `/school/settings?tab=teacher-assignments`

#### الملفات المعدلة:
- `/app/backend/server.py` - إضافة `POST /timetable/generate-smart`
- `/app/backend/routes/timetable_readiness_routes.py` - تحديث `check_teacher_assignments` لقراءة من `teacher_class_assignments`
- `/app/frontend/src/pages/SchoolSettingsPagePro.jsx`:
  - تصميم التبويبات الجديد (grid 8 columns)
  - إضافة `assignmentSubTab`, `classAssignments`, `classAssignmentsLoading`
  - إضافة handlers: `loadClassAssignments`, `handleCreateClassAssignment`, `handleDeleteClassAssignment`
  - إعادة تصميم تبويب "إسناد المعلمين" بقسمين

#### النتائج:
- ✅ نسبة الجاهزية ارتفعت من 89% إلى 96%
- ✅ إسنادات المعلمين: 25/25 (كاملة)
- ✅ توليد الجدول يعمل بنجاح
- ✅ جودة التوزيع: 100%
