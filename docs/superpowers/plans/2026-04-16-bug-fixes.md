# Bug Fixes (High / Medium / Low) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all High, Medium, and Low priority bugs identified in the April 2026 security and code audit.

**Architecture:** Fixes are applied in-place across four groups: backend security hardening, backend performance/reliability, React stale-closure fixes, and Parent Portal data consistency.

**Tech Stack:** FastAPI (Python), React 19, SQLAlchemy/asyncpg, Alembic, Tailwind CSS

---

## Group 1 — Backend Security (High)

### Task 1: Fix tenant isolation in `get_students`
**Files:** `backend/routes/academics_student_routes.py:117-124`

- [x] Reverse the tenant-id / school-id priority so non-admins always get their own school's data, and only platform admins can filter by an arbitrary `school_id` param.

### Task 2: Add tenant check in `get_student`
**Files:** `backend/routes/academics_student_routes.py:277-289`

- [x] After the 404 check, verify the student's `school_id` matches the requesting user's `tenant_id` (skip for platform admins). Return 403 on mismatch.

### Task 3: Restrict options endpoints to staff roles
**Files:** `backend/routes/academics_student_routes.py:146-213`

- [x] Change `get_class_grades_options`, `get_class_teachers_options`, and `get_class_students_options` to use `require_roles([PLATFORM_ADMIN, PRINCIPAL, ADMIN, SUB_ADMIN, TEACHER])` — students and parents can no longer call them.

### Task 4: JWT deny-list on logout
**Files:** `backend/alembic/versions/k1l2m3n4o5p6_add_revoked_tokens_table.py` (new), `backend/pg_models.py`, `backend/dependencies.py`, `backend/routes/auth_routes_mod.py`

- [x] Create `revoked_tokens` table via Alembic migration.
- [x] Add `RevokedToken` SQLAlchemy model to `pg_models.py`.
- [x] Add `jti` (UUID) claim to every access token in `create_access_token`.
- [x] In `get_current_user`, check if the token's `jti` exists in `revoked_tokens` (fail-safe: ignore DB errors).
- [x] In `/auth/logout`, decode the token, extract `jti` + `exp`, and insert a row into `revoked_tokens`.
- [x] Run `alembic upgrade head`.

---

## Group 2 — Backend Performance & Reliability (Low)

### Task 5: Eliminate N+1 query in `analyze_teacher_sessions`
**Files:** `backend/engines/hakim_ai_engine.py:423-438`

- [x] Collect all `class_ids` first, bulk-fetch with a single `gd_find` using `{"id": {"$in": ...}}`, build a lookup dict, then iterate without touching the DB inside the loop.

### Task 6: Add graceful error handling to Hakim AI public methods
**Files:** `backend/engines/hakim_ai_engine.py:359-497`

- [x] Wrap `analyze_teacher_sessions` and `analyze_class_health` in a public/private delegation pattern (`_impl` suffix for internal). The public method catches all exceptions and returns a structured error dict instead of propagating a 500.

### Task 7: Atomic class student counter
**Files:** `backend/routes/academics_student_routes.py:412-417`

- [x] In `transfer_student_class`, replace the `gd_count` + `gd_update_one` pair with `_gd_inc` atomic increments for both old and new class.

---

## Group 3 — React Stale Closures & Race Conditions (Medium)

### Task 8: Fix auto-save stale closure in `SessionTeachPage`
**Files:** `frontend/src/pages/TeacherModule/SessionTeachPage.jsx:280`

- [x] Add `customPositiveBehaviours`, `customNegativeBehaviours`, `customSkills` to the auto-save `useEffect` dependency array.

### Task 9: Add unmount guard to session initialization
**Files:** `frontend/src/pages/TeacherModule/SessionTeachPage.jsx:145-151`

- [x] Introduce `let isMounted = true` and check `if (isMounted)` before calling `loadSessionInfo` in the `.then()` chain. Return cleanup `() => { isMounted = false; }`.

### Task 10: Guard `loadHomeworkStatuses` in `handleSetMode`
**Files:** `frontend/src/pages/TeacherModule/SessionTeachPage.jsx:468`

- [x] Only call `loadHomeworkStatuses()` when `students.length > 0` to prevent silent no-ops when the student list hasn't loaded yet.

### Task 11: Fix `fetchProfile` dependency in `StudentProfilePage`
**Files:** `frontend/src/pages/ParentPortal/StudentProfilePage.jsx:45-57`

- [x] Move `fetchProfile` inside the `useEffect` body and add `api` to the dependency array, eliminating the stale closure and ESLint warning.

---

## Group 4 — Parent Portal Consistency (Medium)

### Task 12: Full re-fetch after absence excuse submission
**Files:** `frontend/src/pages/ParentPortal/ParentAbsenceExcusePage.jsx`

- [x] Replace optimistic `setExcuses(prev => [res.data.excuse, ...prev])` with a call to `fetchData()` after success, and wrap `fetchData` in `useCallback([api])`.

### Task 13: Full re-fetch after meeting request submission
**Files:** `frontend/src/pages/ParentPortal/ParentMeetingRequestPage.jsx`

- [x] Replace optimistic `setMeetings(prev => [...])` with `fetchMeetings()`, and wrap `fetchMeetings` in `useCallback([api])`.

### Task 14: Fix all Parent Portal ESLint `react-hooks/exhaustive-deps` warnings
**Files:** `ChildSchedulePage.jsx`, `ParentChildrenPage.jsx`, `ParentMessagesPage.jsx`, `ParentSettingsPage.jsx`

- [x] For pages where the fetch function is only called in the effect: move it inside the effect body and add `api` to deps.
- [x] For pages where the fetch function is called elsewhere: use `useCallback` + include in dep array.
